#version 330 core

in vec3 position;
in vec3 normal;
in vec2 tex_coord;

out vec2 v_tex;

uniform mat4 M;
uniform mat4 itM;
uniform mat4 V;
uniform mat4 P;
uniform vec3 u_light_pos;

void main(){
    vec4 frag_coord = M*vec4(position, 1.0);
    gl_Position = P*V*frag_coord;
    v_tex = tex_coord;
}